import { SQLDB } from "./sql";
import type { SQLNameMap, SQLQuery } from "./sql";
import type { Dimension } from "../dimension";
export declare class HeavyaiDB extends SQLDB {
    private session;
    constructor(session: any, table: string, nameMap?: SQLNameMap);
    static connectSession(conn: {
        host: string;
        dbName: string;
        user: string;
        password: string;
        protocol: "http" | "https";
        port: number | string;
    }, table: string, nameMap?: SQLNameMap): Promise<HeavyaiDB>;
    protected castBins(input: number): string;
    castTime(name: string): string;
    dimensionExists(_: Dimension): Promise<boolean>;
    tableExists(): Promise<boolean>;
    protected query(q: SQLQuery): Promise<any>;
}
