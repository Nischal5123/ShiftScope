import { Table } from "apache-arrow";
import { SQLDB } from "./sql";
import type { SQLNameMap, SQLQuery } from "./sql";
export declare class HttpDB extends SQLDB {
    private readonly url;
    private readonly encodeGETQuery;
    readonly blocking: boolean;
    constructor(url: string, table: string, encodeGETQuery?: (query: string) => string, nameMap?: SQLNameMap);
    protected query(q: SQLQuery): Promise<Table>;
}
